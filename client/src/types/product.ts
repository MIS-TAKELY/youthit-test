export interface Product {
    _id?: string
    name: string
    description: string
    price: number
    brand: string
    stock: number
    seller: string
    images: string[]
    category: string
}